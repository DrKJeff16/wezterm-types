repos:
  - repo: https://github.com/Lucas-C/pre-commit-hooks
    rev: v1.5.5
    hooks:
      - id: remove-crlf
  - repo: local
    hooks:
      - id: stylua
        name: StyLua
        language: system
        entry: stylua
        types: [lua]
        verbose: true
      - id: check_eof
        name: Run EOF Comments Check
        language: system
        entry: python3 scripts/ensure_eof_comment.py
        types: [lua]
        verbose: true
